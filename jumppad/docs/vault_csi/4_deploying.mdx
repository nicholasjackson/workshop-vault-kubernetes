# Deploying

Time to test your deployment, deploy your application to Kubernetes.

<Task id="deploy_csi">
Deploy the chat application to your Kubernetes cluster using the following command:

```shell
kubectl apply -f chat-csi.yaml
```
</Task>